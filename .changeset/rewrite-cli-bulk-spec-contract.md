---
"@prefactor/cli": minor
---

Point CLI bulk execute at the spec `_type` / `idempotency_key` request items and `outputs` response. Method/path bulk items are no longer accepted.

Bump is minor, not major: `@prefactor/cli` is 0.x (a major changeset would publish 1.0.0). This is the intended level for CLI contract breaks on this branch.
