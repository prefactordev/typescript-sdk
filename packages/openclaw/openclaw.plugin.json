{
  "id": "prefactor",
  "name": "@prefactor/openclaw",
  "version": "0.0.20",
  "description": "Comprehensive lifecycle event monitoring and instrumentation for OpenClaw",
  "runtime": "node",
  "entry": "./dist/index.js",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": {
        "type": "string",
        "description": "Prefactor API URL (e.g. https://app.prefactorai.com)"
      },
      "apiToken": {
        "type": "string",
        "description": "Prefactor API token for authentication"
      },
      "agentId": {
        "type": "string",
        "description": "Agent ID registered in Prefactor"
      },
      "agentName": {
        "type": "string",
        "description": "Human-readable name for this agent shown in the Prefactor UI (e.g. \"My Support Bot\")"
      },
      "agentVersion": {
        "type": "string",
        "description": "Optional user-supplied version suffix for this agent deployment",
        "default": "default"
      },
      "logLevel": {
        "type": "string",
        "enum": ["debug", "info", "warn", "error"],
        "default": "info"
      },
      "userInteractionTimeoutMinutes": {
        "type": "number",
        "description": "Timeout for user interaction spans in minutes (default: 5)",
        "default": 5,
        "minimum": 1,
        "maximum": 60
      },
      "sessionTimeoutHours": {
        "type": "number",
        "description": "Timeout for synthetic session spans in hours (default: 24)",
        "default": 24,
        "minimum": 1,
        "maximum": 168
      }
    },
    "required": []
  },
  "uiHints": {
    "apiUrl": {
      "label": "Prefactor API URL",
      "placeholder": "https://api.prefactor.dev"
    },
    "apiToken": {
      "label": "Prefactor API token",
      "placeholder": "your-api-token",
      "sensitive": true
    },
    "agentId": {
      "label": "Agent ID",
      "placeholder": "your-agent-id"
    },
    "agentName": {
      "label": "Agent Name",
      "placeholder": "OpenClaw Agent"
    },
    "agentVersion": {
      "label": "Agent Version Suffix",
      "placeholder": "default"
    },
    "logLevel": {
      "label": "Log Level",
      "placeholder": "info"
    },
    "userInteractionTimeoutMinutes": {
      "label": "User Interaction Timeout (minutes)"
    },
    "sessionTimeoutHours": {
      "label": "Session Span Timeout (hours)"
    }
  }
}
